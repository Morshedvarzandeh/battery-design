## Summary

Describe the user-visible or engineering outcome and the scope of this change.

## Validation

- [ ] Relevant focused tests pass.
- [ ] `node tools/validate.mjs` passes.
- [ ] Product claims match the surfaces and artifacts actually tested.

## Root-cause memory

Select exactly one defect outcome:

- [ ] No defect resolved; no root-cause record is required.
- [ ] Defect resolved; this pull request links an existing record or adds/updates one.

Root-cause record: `N/A` or a stable id such as `rc-fmi-representation-drift`.

When a defect is resolved:

- [ ] The existing record is still accurate, or I add or update the record.
- [ ] The record states the underlying root cause and implemented resolution.
- [ ] The record includes recurrence prevention and an automated regression test.
- [ ] `npm run test:root-causes` passes.

## Release boundary

State any untested platform, proprietary host, certification, deployment or
runtime boundary that remains outside this pull request.
