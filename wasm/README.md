# Generated Rust core

`npm run wasm:build` compiles `rust-core/` into
`wasm/battery_design_core.wasm`. The binary is generated in CI, the GitHub
Pages deployment, and desktop release builds; it is not committed.

