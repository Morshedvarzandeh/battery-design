# FMI enterprise signal map

> Generated by `node tools/generate-fmi-signal-docs.mjs --write` from
> `js/fmi-signal-map.js`. Do not edit this file by hand. Validate it with
> `node tools/generate-fmi-signal-docs.mjs --check`.

This is the stable, host-facing scalar contract for the battery-design FMI 2.0
Co-Simulation component. Value references are an append-only ABI: existing
references must not be renamed or renumbered. A contract-version or checksum
change requires a new host-mapping review.

## Contract identity

- Document format: `battery-design/fmi-signal-document@1`
- Scalar contract format: `battery-design/fmi-io-contract@1`
- Scalar contract version: `1.0.0`
- Scalar contract SHA-256: `5b63861f581ef668dd0e9b8eb26bfa4d7936ca4d92dd89ef02e2e57b10640a50`
- FMI interface: `2.0` Co-Simulation (`Real` scalars only)

The checked-in document describes the generic ABI. Every exported FMU also
contains `resources/battery-design-io-map.json`, which binds this contract to
that artifact's GUID, revision and fixed parameter starts.

## Fixed parameters

The host may set these parameters during instantiation or initialization. They
are fixed after initialization. A governed DesignSpec export resolves their
starts from one validated design and the reduced-model coefficients; the XML,
generated C defaults and machine I/O map carry the same values.

| VR | Stable role | FMI name | Unit | Display | Start | Constraint | Design/model binding | Meaning and sign |
|---:|---|---|---|---|---|---|---|---|
| 1 | `battery.pack.seriesCount` | `cells_series` | `1` | — | design-bound | >= 1 | `design.pack.s` | Number of cells connected in series in the exported pack. Positive integer count; the value has no signed direction. |
| 2 | `battery.pack.parallelCount` | `cells_parallel` | `1` | — | design-bound | >= 1 | `design.pack.p` | Number of cells connected in parallel in the exported pack. Positive integer count; the value has no signed direction. |
| 3 | `battery.cell.ratedCapacity` | `capacity_Ah` | `A.h` | — | design-bound | > 0 | `design.cell.capacityAh` | Rated capacity of one cell; pack capacity is this value times cells_parallel. Positive magnitude; zero or negative capacity is outside the physical contract. |
| 4 | `battery.cell.ocvMinimum` | `ocv_min` | `V` | — | design-bound | > 0 | `design.cell.vMin` | Cell open-circuit voltage at zero state of charge. Positive cell potential in the pack discharge polarity. |
| 5 | `battery.cell.ocvMaximum` | `ocv_max` | `V` | — | design-bound | > 0; > ocv_min | `design.cell.vMax` | Cell open-circuit voltage at full state of charge. Positive cell potential in the pack discharge polarity. |
| 6 | `battery.cell.ohmicResistanceReference` | `r0_mOhm` | `mOhm` | — | design-bound | >= 0 | `simulation.params.r0Ref` | Cell ohmic series resistance at the reference temperature. Non-negative resistance magnitude. |
| 7 | `battery.cell.rcBranchResistance` | `rc1_mOhm` | `mOhm` | — | design-bound | >= 0 | `simulation.params.rc1R` | Cell resistance of the reduced model first RC polarization branch. Non-negative resistance magnitude. |
| 8 | `battery.cell.rcBranchTimeConstant` | `rc1_tau_s` | `s` | — | design-bound | > 0 | `simulation.params.rc1TauS` | Time constant of the reduced model first RC polarization branch. Positive duration; zero is outside the physical contract. |
| 9 | `battery.cell.resistanceActivationEnergy` | `r0_Ea_J` | `J/mol` | — | design-bound | >= 0 | `simulation.params.r0EaJ` | Activation energy used by the Arrhenius resistance-temperature relation. Non-negative activation-energy magnitude. |
| 10 | `battery.cell.specificHeatCapacity` | `cp_cell` | `J/(kg.K)` | — | design-bound | > 0 | `simulation.params.cpCellJkgK` | Specific heat capacity of one cell used by the lumped thermal node. Positive material-property magnitude; zero is outside the physical contract. |
| 11 | `battery.cell.mass` | `mass_cell_kg` | `kg` | — | design-bound | > 0 | `design.cell.massG / 1000` | Mass of one cell used to derive the lumped pack heat capacity. Positive mass magnitude; zero is outside the physical contract. |
| 12 | `battery.thermal.coolantConductance` | `h_cool_WK` | `W/K` | — | design-bound | >= 0 | `simulation.params.hCoolWK (direct reduced-model value)` | hCoolWK applied as total pack-to-coolant conductance in the current single-node reduction; it is not multiplied by module count. Non-negative heat-transfer conductance. |
| 13 | `battery.thermal.ambientConductance` | `ua_amb_WK` | `W/K` | — | design-bound | >= 0 | `simulation.params.uaAmbWK` | Pack-to-ambient conductance used by the lumped thermal node. Non-negative heat-transfer conductance. |
| 14 | `battery.cell.entropicVoltageCoefficient` | `entropy_VK` | `V/K` | — | design-bound | finite | `simulation.params.entropyVK` | Cell open-circuit-voltage temperature coefficient dU/dT for reversible heat. Signed dU/dT; its sign controls the reversible heating term for a given current direction. |

## Runtime ports

Inputs are written by the co-simulation master before a communication step and
held over that step. Outputs are read-only and are recalculated on reset,
initialization exit and each successful communication step.

| Direction | VR | Stable role | FMI name | Unit | Display | Start | Meaning and sign |
|---|---:|---|---|---|---|---|---|
| Host -> FMU | 15 | `battery.pack.terminalCurrent` | `I_pack` | `A` | — | `0` | Pack terminal current supplied by the host model. Positive current discharges the pack and leaves its positive terminal; negative current charges it. |
| Host -> FMU | 16 | `battery.environment.ambientTemperature` | `T_ambient` | `degC` | — | `25` | Ambient temperature; the current reduced model also uses it as the coolant inlet reference. Signed Celsius value; conversion to absolute temperature is internal to the model. |
| Host -> FMU | 17 | `battery.coolant.massFlow` | `coolant_flow` | `kg/s` | — | `0.05` | Coolant mass flow through the reduced thermal boundary; zero disables coolant heat removal. Non-negative forward mass-flow magnitude; reverse flow is not represented. |
| FMU -> Host | 18 | `battery.pack.terminalVoltage` | `V_pack` | `V` | — | calculated | Pack terminal voltage under the applied current and current reduced-model state. Positive from the pack negative terminal to its positive terminal. |
| FMU -> Host | 19 | `battery.pack.stateOfCharge` | `SoC` | `1` | `%` | calculated | Pack state of charge as a fraction from empty (0) to full (1). Dimensionless fraction; discharge current decreases it and charge current increases it. |
| FMU -> Host | 20 | `battery.cell.representativeTemperature` | `T_cell` | `degC` | — | calculated | Representative temperature of the single lumped cell node. Signed Celsius value; conversion to absolute temperature is internal to the model. |
| FMU -> Host | 21 | `battery.pack.netInternalHeatSource` | `Q_loss` | `W` | — | calculated | Net internal pack heat source: irreversible losses plus signed reversible entropic heat. Positive heats the lumped cell node; reversible heat can make the net value negative. |
| FMU -> Host | 22 | `battery.cell.uniformTerminalVoltageEstimate` | `V_cell_min` | `V` | — | calculated | Uniform-cell terminal-voltage estimate V_pack/cells_series; not a resolved cell minimum. Positive in the pack discharge polarity; follows the sign of V_pack. |
| FMU -> Host | 23 | `battery.pack.terminalPower` | `P_terminal` | `W` | — | calculated | Electrical terminal power V_pack multiplied by I_pack. Positive is power delivered by the pack on discharge; negative is power absorbed on charge. |

## Versioned host mapping matrix

- Mapping format: `battery-design/fmi-host-mapping@1`
- Mapping revision: `1`
- Applies to scalar contract: `battery-design/fmi-io-contract@1/1.0.0`
- Applies to contract checksum: `5b63861f581ef668dd0e9b8eb26bfa4d7936ca4d92dd89ef02e2e57b10640a50`

The matrix is deliberately host-neutral. Twin Builder, Simulink, GT-SUITE and
other FMI 2.0 masters consume the names and value references declared in
`modelDescription.xml`; product UI labels or adapters may differ. Those local
labels must be recorded in the product/version-specific acceptance evidence,
not added as an unverified second signal contract here.

| Mapping ID | Host responsibility | FMI port | VR | Unit / display | Conversion policy |
|---|---|---|---:|---|---|
| `runtime.battery.pack.terminalCurrent` | Write `battery.pack.terminalCurrent` before each communication step and hold it over the step. | `I_pack` | 15 | `A` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.environment.ambientTemperature` | Write `battery.environment.ambientTemperature` before each communication step and hold it over the step. | `T_ambient` | 16 | `degC` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.coolant.massFlow` | Write `battery.coolant.massFlow` before each communication step and hold it over the step. | `coolant_flow` | 17 | `kg/s` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.pack.terminalVoltage` | Read `battery.pack.terminalVoltage` after initialization and each successful communication step. | `V_pack` | 18 | `V` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.pack.stateOfCharge` | Read `battery.pack.stateOfCharge` after initialization and each successful communication step. | `SoC` | 19 | `1` / `%` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.cell.representativeTemperature` | Read `battery.cell.representativeTemperature` after initialization and each successful communication step. | `T_cell` | 20 | `degC` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.pack.netInternalHeatSource` | Read `battery.pack.netInternalHeatSource` after initialization and each successful communication step. | `Q_loss` | 21 | `W` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.cell.uniformTerminalVoltageEstimate` | Read `battery.cell.uniformTerminalVoltageEstimate` after initialization and each successful communication step. | `V_cell_min` | 22 | `V` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |
| `runtime.battery.pack.terminalPower` | Read `battery.pack.terminalPower` after initialization and each successful communication step. | `P_terminal` | 23 | `W` | Explicit only; preserve the documented sign and do not rely on an implicit host conversion. |

## Static design/layout data is not a runtime port

`resources/battery-design-design.json` is the immutable design snapshot bound
to the FMU GUID. It records the source kind and completeness, DesignSpec and
semantic checksums, cell/application identity, S/P architecture, module
partition, mass, energy, resistance, voltage range, dimensions, layout summary
and selected component IDs. It is provenance for the exported plant and may be
read as a resource by tooling.

That JSON resource is not an FMI scalar interface, is not updated while the
component steps, and must not be mistaken for live telemetry. Runtime coupling
uses only the 3 inputs and 6 outputs above. Adding a live layout,
module-temperature or BMS-register signal requires an appended, versioned FMI
contract variable.

## Reduced-plant and thermal-boundary semantics

The component is a system-level reduced plant: linear cell OCV between the
declared voltage limits, R0 plus one RC polarization branch, and Arrhenius
temperature dependence. It is not a P2D electrochemical, degradation, cell-
imbalance, CFD or spatial thermal model. `V_cell_min` is therefore the uniform
estimate `V_pack / cells_series`, not a resolved minimum across cells.

There is one representative lumped temperature state, `T_cell`. The
`h_cool_WK` parameter is the total pack-to-coolant conductance in this
reduction; it is not a per-module value and is not multiplied by module count.
`ua_amb_WK` is total pack-to-ambient conductance. `T_ambient` supplies both
the ambient and current coolant-inlet reference. `coolant_flow` is a
non-negative forward mass-flow magnitude: zero disables coolant heat removal,
while positive flow is reduced through a finite-capacity-rate epsilon-NTU
boundary. `Q_loss` is the net internal heat source, including irreversible
losses and signed reversible entropic heat, so it can be negative.

## Change control

1. Change the canonical descriptors only in `js/fmi-signal-map.js`.
2. Append new value references; never reuse or renumber a published reference.
3. Regenerate these documents and the artifact machine map.
4. Run contract, native lifecycle and packaging tests.
5. Repeat every product/version-specific commercial-host acceptance record for
   the new contract checksum and exact FMU SHA-256.
